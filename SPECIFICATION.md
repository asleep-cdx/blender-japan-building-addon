# 日本住宅モデラー v0.1

## Build 01 — アドオン基盤・UI実装指示書

あなたはBlender 5.2 LTS向けのBlenderアドオン開発担当です。

以下の仕様に従って、Blender 5.2 LTSで動作するアドオンのBuild 01を実装してください。

---

# 1. プロジェクトの目的

このアドオンは、将来的に日本住宅の間取り図から、寸法の整合した室内3Dモデルを構築するためのものです。

最終的には、

* 壁
* 床
* 天井
* ドア
* 窓
* 住宅設備
* 実寸アセット
* 間取り図の解析

などを扱えるようにします。

ただし、今回のBuild 01ではこれらを実装しません。

今回の目的は、

> **今後のWall Systemを実装するためのアドオン基盤とUIを作ること**

です。

---

# 2. 対象環境

* Blender 5.2 LTS
* Python 3.x（Blender 5.2 LTSに内蔵されているPythonを使用）
* Windows環境を想定
* Blender標準APIのみを基本とする
* 外部Pythonパッケージへの依存は作らない

Blender 5.2 LTSのAPI仕様を前提として実装してください。

古いBlenderのAPIを前提にしたコードは避けてください。

---

# 3. Build 01で実装する範囲

今回実装するのは以下のみです。

1. Blenderからインストール可能なアドオン構造
2. アドオン名「日本住宅モデラー」
3. Blender UIへのパネル追加
4. 新規Wall用のデフォルト値設定
5. 選択中のWallの情報を表示するためのUI基盤
6. 壁厚の数値プロパティ
7. 壁高さの数値プロパティ
8. 将来のWall Systemに拡張できるPropertyGroup構造

---

# 4. 今回は実装しない機能

以下は絶対に今回のBuild 01へ実装しないでください。

* 壁のメッシュ生成
* マウスクリックによる壁作成
* 始点・終点の取得
* スナップ
* 壁同士の接合
* 壁の交差処理
* 間取り図の読み込み
* AI画像認識
* ドア
* 窓
* 床
* 天井
* 住宅設備
* アセットブラウザ
* LIXIL等のメーカー製品
* 外部API
* AI API
* Geometry Nodesによる実装

Build 01では「基盤とUI」に限定してください。

---

# 5. アドオン名

表示名：

「日本住宅モデラー」

内部モジュール名は英語で、

`japanese_house_modeler`

を使用してください。

Blender上でのアドオン名とPythonモジュール名は分離してください。

---

# 6. 推奨フォルダ構造

将来的な拡張を考慮し、最初から以下のような構造を採用してください。

```text
japanese_house_modeler/
    __init__.py
    properties.py
    ui.py
    operators.py
```

Build 01ではこの程度で構いません。

将来的には、

```text
japanese_house_modeler/
    __init__.py
    properties.py
    ui.py
    operators.py

    wall/
    door/
    window/
    floor/
    ceiling/
    asset/
    floorplan/
```

のように拡張できる構造を想定しています。

---

# 7. 単位

ユーザーインターフェース上では寸法をmmで扱います。

例：

```text
105 mm
2500 mm
```

Blender内部では必要に応じてmへ変換して構いません。

ただし、UI上でユーザーにm単位を要求しないでください。

---

# 8. Wall用PropertyGroup

将来のWall Systemで使用するためのPropertyGroupを作成してください。

最低限、以下のプロパティを用意してください。

```text
wall_thickness
wall_height
```

型は適切なBlender Propertyを使用してください。

初期値：

```text
wall_thickness = 105 mm
wall_height = 2500 mm
```

---

# 9. 数値入力の制約

不正な値を入力できないようにしてください。

例えば、

```text
wall_thickness <= 0
wall_height <= 0
```

は許可しないでください。

適切なmin/max値を設定してください。

ただし、住宅の仕様を考慮して過剰に狭い制限値を設定しないでください。

例えば壁厚について、

105mmだけしか入力できない、

というような仕様にはしないでください。

将来的に戸建て・マンション・外壁・間仕切り等を扱うため、ユーザーが任意の妥当なmm値を入力できるようにしてください。

---

# 10. UI

Blenderの3D Viewportのサイドバー（Nパネル）に、

「日本住宅モデラー」

というパネルを追加してください。

カテゴリ名は、

`日本住宅`

など、分かりやすいものにしてください。

UIの概念は以下です。

```text
日本住宅モデラー
────────────────

WALL

新規壁
  壁厚
  [ 105 ] mm

  壁高さ
  [ 2500 ] mm

  [ ＋ 壁 ]

────────────────

選択中の壁

  壁厚
  [ --- ] mm

  壁高さ
  [ --- ] mm
```

ただし、Build 01では「＋ 壁」を押しても壁は生成しません。

ボタンは存在させても構いませんが、

```text
Wall creation is not implemented in Build 01.
```

など、未実装であることが分かる状態にしてください。

可能ならボタン自体を無効化してください。

---

# 11. 「新規壁」と「選択中の壁」を分離する

これは重要です。

新しく作成する壁のデフォルト値：

```text
wall_thickness
wall_height
```

と、

現在選択されているWallのパラメータは別管理にしてください。

将来的には、

```text
新規壁
  デフォルト壁厚
  デフォルト壁高さ

選択中の壁
  個別壁厚
  個別壁高さ
```

となります。

Build 01では選択中Wallの実装はUI基盤までで構いません。

---

# 12. 将来のWallデータ構造

将来的なWallオブジェクトは概念的に以下とします。

```text
Wall
├── Start
├── End
├── Thickness
├── Height
└── Connection Information
```

ただしBuild 01では、

* Start
* End
* Connection Information

はまだ実装しなくて構いません。

今回作るPropertyGroupが将来拡張可能な構造になるようにしてください。

---

# 13. Lengthについて

重要：

WallのLengthは基本データとして保存しない方針です。

将来は、

```text
Start
End
↓
Length = distance(Start, End)
```

として計算します。

そのため、Build 01ではLengthプロパティを実装する必要はありません。

---

# 14. 将来の座標系

Wallは将来的に、

```text
Start = (X1, Y1, 0)
End   = (X2, Y2, 0)
```

というXY平面上の中心線として定義します。

Z方向は壁高さです。

つまり、

```text
XY = 壁芯の位置
Z  = 高さ
```

です。

Build 01ではまだStart/Endを実装しません。

---

# 15. 壁基準線

将来的なWall Systemでは、

> **壁の中心線（壁芯）を基準線とする。**

これを変更しないでください。

壁厚が105mmなら、中心線から左右52.5mmずつ広がる構造を想定します。

壁厚が150mmなら、中心線から左右75mmずつ広がります。

---

# 16. コーディング上の方針

以下を守ってください。

* Blender 5.2 LTSで動作するコードを書く
* Blender標準APIを使用
* 外部ライブラリを追加しない
* 不要に複雑な設計をしない
* 将来のWall / Door / Window / Assetへの拡張性を確保
* グローバル変数を乱用しない
* PropertyGroupを適切に利用する
* register / unregisterを正しく実装する
* Blender再起動後も正常にアドオンを読み込める構造にする
* アドオンを無効化した場合に登録したクラスやプロパティを正しく解除する
* Blenderの既存データを破壊しない

---

# 17. コメント

コードには必要な箇所だけコメントを付けてください。

特に、

* なぜPropertyGroupをこの構造にしたのか
* 将来Wall Systemをどう拡張する想定なのか
* mmとBlender内部単位の変換をどう扱っているか

についてはコメントを残してください。

---

# 18. README

可能ならアドオンフォルダにREADME.mdを作成してください。

READMEには、

* アドオンの目的
* Blender 5.2 LTS対応
* Build 01で実装済みの機能
* Build 01で未実装の機能
* インストール方法
* 今後の開発予定

を簡潔に記載してください。

---

# 19. 完成後の報告

実装が完了したら、以下を報告してください。

1. 作成したファイル一覧
2. 各ファイルの役割
3. Blender 5.2 LTSでのインストール方法
4. Blender上でUIを表示する方法
5. Build 01で実装された機能
6. 未実装機能
7. 実装上の判断事項
8. エラーや懸念事項
9. 次のBuild 02で何を実装するべきか

---

# 20. Build 01の完了条件

以下をすべて満たしたらBuild 01完了としてください。

* Blender 5.2 LTSにアドオンとしてインストールできる
* アドオンを有効化できる
* 3D ViewportのNパネルに「日本住宅モデラー」が表示される
* 壁厚105mmが初期値として表示される
* 壁高さ2500mmが初期値として表示される
* 数値を変更できる
* 不正な負数等を適切に防止できる
* register/unregisterが正常に動作する
* Blenderを再起動してもアドオンを再度有効化できる
* まだ壁メッシュが生成されないこと
* 将来Wall Systemを追加できる構造になっている

---

# 21. 次のBuildについて

Build 01が正常に動作した後、次のBuild 02では、

> **トップビューで始点をクリック → 終点をクリック → Wallオブジェクトを生成**

する機能を実装する予定です。

Build 02では、

* XY平面へのマウス座標変換
* Start / End
* 壁芯
* 壁厚
* 壁高さ
* 壁メッシュ生成

を実装対象とします。

ただし、Build 01のテストが完了するまではBuild 02を実装しないでください。

---

# 22. 最重要事項

このプロジェクトでは、単なる「Blender上の壁メッシュ生成」ではなく、

> **日本住宅の寸法を正確に扱えるパラメトリックな建築モデリングシステム**

を最終目標としています。

したがって、目先の見た目だけを優先して将来のデータ構造を壊す実装は避けてください。

一方で、Build 01では過剰設計もしないでください。

**今回はアドオン基盤とUIだけを完成させることが目的です。**

実装前に仕様上の矛盾やBlender 5.2 LTS API上の問題を発見した場合は、勝手に仕様を変更せず、

1. 問題点
2. なぜ問題なのか
3. 推奨する変更案

を提示してください。

その場合はコードを書き始める前に報告してください。
