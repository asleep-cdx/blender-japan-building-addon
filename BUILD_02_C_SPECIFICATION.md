# 日本住宅モデラー — Build 02-C Specification

## 1. 目的

Build 02-Cでは、Build 02-Bまでに実装した壁作図機能へ、**既存Wallの壁芯端点（start / end）へのスナップ**を追加する。

目的は、複数のWallを正確に同一点へ接続できるようにし、後続の壁接合・出隅・入隅処理の基礎を作ることである。

Build 02-Cでは「端点を正確に一致させる」ことだけを扱い、壁メッシュ同士の自動トリミングや接合形状の生成は行わない。

---

## 2. 親仕様

本仕様書は以下を前提とする。

- `SPECIFICATION.md`
- `BUILD_02_A_SPECIFICATION.md`
- `BUILD_02_B_SPECIFICATION.md`

既存仕様と矛盾する場合は、勝手に仕様変更せず問題点を明示する。

---

## 3. Build 02-Cで実装する範囲

Build 02-Cでは以下を実装する。

- 既存Wallの `start` / `end` へのスナップ
- 始点のスナップ
- 終点候補のスナップ
- 画面ピクセル基準のスナップ判定
- スナップ対象端点のハイライト表示
- 始点確定前のスナップ候補ハイライト
- Build 02-BのShift 15°角度拘束との優先順位制御
- スナップ後の最終端点をWallデータへ正確に保存
- Build 02-A / 02-BのUndo・キャンセル・壁生成・プレビュー等の維持

---

## 4. Build 02-Cで実装しない範囲

以下はBuild 02-Cでは実装しない。

- 既存Wallの途中（線分上）へのスナップ
- グリッドスナップ
- 壁面・メッシュ頂点への一般スナップ
- 壁同士の自動トリミング
- 壁同士の自動延長
- 壁芯交点の自動生成
- T字接合処理
- 十字接合処理
- 出隅・入隅処理
- 壁厚を考慮したコーナー処理
- 既存Wallの端点編集
- 既存Wallの壁厚・壁高さ変更と再生成
- ドア・窓・床・天井
- 間取り図の自動解析
- その他Build 02-D以降の機能

---

## 5. スナップ対象

スナップ対象は、日本住宅モデラーが管理する既存Wallの壁芯端点のみとする。

対象条件：

- 現在のView Layerで表示対象になっているObject
- `object.jhm_wall.is_wall == True`
- `object.jhm_wall.start`
- `object.jhm_wall.end`

スナップ座標の情報源は、メッシュ頂点ではなく `jhm_wall.start` / `jhm_wall.end` とする。

Wallの長さや端点をメッシュ形状から逆算してはならない。

---

## 6. 非表示Wallの扱い

以下のWallはスナップ対象外とする。

- Viewportで非表示のWall
- 非表示Collection内のWall
- 現在のView Layerから除外されているWall
- Local View等で現在見えていないWall

実装では、現在の3D Viewでユーザーが視認できるWallを優先して扱う。

選択状態は問わない。

---

## 7. スナップ距離

スナップ判定は実寸mmではなく、**画面上のピクセル距離**で行う。

初期固定値：

`12 px`

既存Wall端点の3Dワールド座標を画面2D座標へ投影し、現在のraw候補位置との画面距離を比較する。

12 px以内に候補が存在する場合、その端点をスナップ候補とする。

この値はBuild 02-CではUIから変更できる設定項目にしない。

将来必要になった場合のみ設定化する。

---

## 8. 画面外・投影不能端点

以下の端点は候補から除外する。

- `location_3d_to_region_2d()` 等で画面座標へ投影できない端点
- 現在のViewから見て有効な2D位置を取得できない端点

画面外・背面等の端点へ意図せずスナップしてはならない。

---

## 9. 複数候補がある場合

12 px以内に複数のWall端点が存在する場合は、**画面上でraw候補に最も近い端点**を採用する。

優先基準：

1. 画面上の距離が最小の端点
2. 完全な同距離の場合は安定した決定順を使い、マウスを動かしていないのに候補が交互に切り替わることを避ける

同一ワールド座標に複数Wall端点が重なっている場合は、どのWall由来であっても最終座標が同一なら問題ない。

---

## 10. 始点スナップ

`＋ 壁` を押した直後、まだ始点が確定していない状態でも既存Wall端点スナップを有効にする。

raw XY位置が既存Wall端点から12 px以内に入った場合、

- その端点をスナップ候補として認識
- ハイライト表示
- 左クリックした場合、その端点座標を新しいWallの `start` として確定

する。

始点確定時にはShift角度拘束を適用しない。

---

## 11. 始点確定前のハイライト

まだ始点をクリックしていない状態でも、スナップ対象端点の近くへカーソルを移動した場合、対象端点をハイライトする。

この状態では壁芯ラインはまだ描画しない。

表示イメージ：

`◎`

または通常の端点表示より明確な二重円とする。

これにより、ユーザーはクリック前に「どの端点へ吸着するか」を確認できる。

---

## 12. 終点スナップ

始点確定後は、現在の終点候補について既存Wall端点スナップを行う。

raw XY位置が既存Wall端点から12 px以内に入った場合、

- 終点候補をその既存Wall端点へ一致させる
- プレビュー壁芯線の終点もその座標へ移動する
- 対象端点をハイライトする
- 左クリック時、その正確な座標を新しいWallの `end` として保存する

プレビューと確定結果で別のスナップ判定を使ってはならない。

最終クリック時にも候補を再評価し、確定結果とプレビューの不一致を避ける。

---

## 13. スナップとShift角度拘束の優先順位

優先順位は以下とする。

**既存Wall端点スナップ ＞ Shift 15°角度拘束**

処理上の考え方：

`raw XY`
→ 既存Wall端点スナップ判定
→ スナップ成功なら、その端点を最終候補として維持
→ スナップ失敗なら、Shift押下中のみ15°角度拘束
→ preview / final

スナップ成功時は、Shiftが押されていてもスナップ座標を角度拘束によって移動させてはならない。

---

## 14. Shift併用時の具体的挙動

### 14.1 スナップ対象がない場合

Shift未押下：

- 自由角度

Shift押下：

- Build 02-Bと同じ15°刻み拘束

### 14.2 スナップ対象がある場合

Shift未押下：

- 既存Wall端点へスナップ

Shift押下：

- 既存Wall端点へスナップ
- 15°拘束よりスナップを優先
- スナップ座標を変更しない

### 14.3 スナップ範囲から外れた場合

カーソルが12 px範囲から外れた瞬間、

- Shift押下中なら15°角度拘束へ戻る
- Shift未押下なら自由角度へ戻る

マウス移動に応じて即座に更新する。

---

## 15. Shift押下・解放時の即時更新

Build 02-Bの仕様を維持する。

始点確定後、マウスを動かさなくてもShift押下・解放によってプレビューを再評価する。

ただしスナップ中は端点スナップが最優先である。

例：

- スナップ中にShiftを押す → スナップ位置のまま
- スナップ中にShiftを離す → スナップ位置のまま
- スナップ範囲外でShiftを押す → 15°拘束へ即時変更
- スナップ範囲外でShiftを離す → 自由角度へ即時変更

---

## 16. スナップハイライト

ユーザーが「どこにスナップされるか」を明確に確認できるよう、スナップ候補端点を通常マーカーより強調する。

推奨表示：

- 通常端点マーカー：約5〜6 px半径
- スナップ対象：通常マーカーに加えて外側へ約9 px半径の追加リング
- 通常より太い線または視認性の高い強調表示
- 二重円 `◎` として見えること

ハイライトはGPUによる画面表示のみとする。

ハイライト目的でObject / Mesh / Empty等を生成してはならない。

---

## 17. 始点がスナップされた場合の表示

始点が既存Wall端点へスナップして確定された場合、作図中はその始点についてスナップ済みであることが分かる表示を維持してよい。

推奨：

- 始点：`◎`
- 終点通常候補：`○`
- 終点スナップ中：`◎`

例：

`◎────────────────○`

または

`◎────────────────◎`

Wall確定後は一時的なハイライトを消す。

---

## 18. 候補解決責務

Build 02-A / 02-Bで用意した候補解決の責務分離を維持する。

基本構造：

`raw XY`
→ `snap_endpoint_candidate()`
→ `resolve_endpoint_candidate()`
→ `preview / final`

Build 02-Cでは、

- `snap_endpoint_candidate()` が既存Wall端点スナップを担当する
- `resolve_endpoint_candidate()` がBuild 02-BのShift角度拘束を担当する
- スナップ成功状態が `resolve_endpoint_candidate()` に伝わり、スナップ成功時は角度拘束を適用しない

実装方法は、既存構造を大きく崩さない範囲で決めてよい。

不要なクラス追加や大規模リファクタリングは行わない。

---

## 19. 始点への候補解決

Build 02-B以前では最初の左クリック位置をそのまま `start` に使用していた。

Build 02-Cでは最初の左クリックにも既存Wall端点スナップを適用する。

ただし始点にはまだ角度基準となる既存startがないため、

- 始点：スナップのみ
- 終点：スナップ + 必要に応じて角度拘束

とする。

---

## 20. 最小長判定

Build 02-Bと同様、最終的に決定した始点Sと終点Eに対して判定する。

`distance(E - S) > 1e-6 m`

を有効条件とする。

同じ既存端点を始点・終点の両方にスナップした場合など、結果が最小長以下になる場合はWallを生成しない。

この値は建築上の最小寸法ではなく、退化メッシュ防止の技術的しきい値である。

---

## 21. 保存するWallデータ

Wall確定時には、最終的に解決された正確な値を保存する。

- `wall.start`
- `wall.end`
- `wall.wall_thickness`
- `wall.wall_height`
- `wall.is_wall`

スナップした場合、`start` / `end` は対象既存Wallの保存済み端点座標と一致すること。

「見た目だけ近い」状態にしてはならない。

---

## 22. 確定Wallメッシュ

確定Wallメッシュ生成はBuild 02-A / 02-Bと同じ仕様を維持する。

- 壁芯基準
- Start / Endは端面中心
- 壁厚は壁芯から左右へ1/2ずつ
- 高さはZ=0から上方向
- 作図開始時の壁厚・高さを使用
- 一つのWall = 一つのObject

端点スナップのために確定メッシュ生成方式を変更してはならない。

---

## 23. パフォーマンス

Build 02-Cでは、既存Wallの `start` / `end` を走査する単純な実装でよい。

ただし、

- メッシュ全頂点を走査しない
- depsgraphから不要なジオメトリを生成しない
- 一時Objectを作らない

こと。

将来Wall数が大幅に増え、性能上の問題が確認された場合にのみ空間インデックス等を検討する。

---

## 24. Wall Objectの手動Transformについて

Build 02-Cでも `jhm_wall.start` / `jhm_wall.end` をワールド座標のSource of Truthとして扱う。

ユーザーがBlender標準のMove / Rotate / ScaleでWall Objectだけを手動変形した場合、メッシュと保存済み端点が不一致になる可能性がある。

この問題の解決はBuild 02-Cの範囲外とする。

端点スナップは保存済み `start` / `end` を使用する。

---

## 25. Build 02-B仕様の維持

以下を変更しない。

- Shift未押下時の自由角度
- Shift押下時の15°刻み拘束
- Shift押下・解放時の即時更新
- 細い壁芯ラインによるプレビュー
- 始点・終点の円形マーカー
- 画面ピクセル基準のプレビュー
- 前面表示
- 確定前のObject / Mesh非生成

ただし、スナップ成功時のみ「端点スナップ ＞ Shift拘束」の優先順位を適用する。

---

## 26. Build 02-A仕様の維持

以下を変更しない。

- XY平面へのレイ交差
- `is_wall`
- `start`
- `end`
- `wall_thickness`
- `wall_height`
- 壁芯基準の確定メッシュ
- 作図開始時の壁厚・壁高さスナップショット
- Undo
- Wall生成後の選択・active化
- Esc / 右クリックによるキャンセル
- 選択中Wall情報のread-only表示
- 技術的最小長判定

---

## 27. 変更対象ファイル

Build 02-Cの主要実装対象は原則として、

- `japanese_house_modeler/operators.py`

とする。

新規仕様書として、

- `BUILD_02_C_SPECIFICATION.md`

を追加する。

`__init__.py`
`properties.py`
`ui.py`

は、Build 02-Cの要件上必要がない限り変更しない。

不要なファイル変更・整形・リファクタリングを行わない。

---

## 28. 実機テスト項目

Blender 5.2 LTSで最低限以下を確認する。

### A. 始点スナップ

1. 既存Wallを1本作成
2. `＋ 壁` を開始
3. 既存Wall端点へカーソルを近づける
4. クリック前に対象端点がハイライトされる
5. クリックすると新規Wallの始点が既存Wall端点へ一致する

### B. 終点スナップ

1. 始点を任意位置で確定
2. 終点を既存Wall端点へ近づける
3. 対象端点がハイライトされる
4. プレビュー壁芯線がその端点へ吸着する
5. クリックすると新規Wallの終点が正確に一致する

### C. Shiftとの優先順位

1. Shiftを押しながら作図する
2. 既存Wall端点へ近づける
3. 15°拘束より端点スナップが優先される
4. スナップ中にShiftを押す・離す
5. マウスを動かさなくてもスナップ位置が維持される

### D. スナップ解除

1. 端点へスナップさせる
2. カーソルを12 px範囲外へ移動する
3. Shift押下中なら15°拘束へ戻る
4. Shift未押下なら自由角度へ戻る

### E. 複数端点

1. 近接する複数のWall端点を用意
2. カーソルに最も近い端点が選ばれる
3. 候補が不必要にちらつかない

### F. 非表示Wall

1. WallをViewportで非表示にする
2. 非表示Wallの端点へスナップしないことを確認する

### G. ズーム

1. Viewportをズームイン・ズームアウト
2. スナップ半径の操作感が画面上でほぼ一定であることを確認する

### H. 回帰テスト

以下がBuild 02-Bまでと同様に動作すること。

- 自由角度作図
- Shift 15°拘束
- Shift押下・解放の即時反映
- 壁芯ライン＋円形マーカー
- Escキャンセル
- 右クリックキャンセル
- Ctrl+Z
- 壁厚
- 壁高さ
- Wall生成後の選択
- XY平面への作図

---

## 29. 完了条件

Build 02-Cは、Blender 5.2 LTS実機で以下を満たした時点で完了とする。

- 始点が既存Wall端点へ正確にスナップできる
- 終点が既存Wall端点へ正確にスナップできる
- スナップ半径が画面基準12 pxで機能する
- スナップ候補が分かりやすくハイライトされる
- 始点クリック前にもスナップ候補を確認できる
- スナップがShift 15°拘束より優先される
- スナップ範囲を外れたら自由作図またはShift拘束へ自然に戻る
- 保存された `start` / `end` が対象端点座標と正確に一致する
- 非表示Wallへスナップしない
- Build 02-A / 02-Bの既存機能に回帰不具合がない
- 確定前に不要なObject / Meshを生成しない

---

## 30. 次段階への接続

Build 02-C完了後は、端点が正確に共有できる状態を基礎として、次の候補を検討する。

- 既存Wallの壁厚・壁高さ編集と再生成
- Wall端点編集
- 壁同士の接続情報
- 壁芯交点処理
- T字接合
- 出隅・入隅
- 壁厚を考慮した自動トリミング

Build 02-Cではこれらを先取り実装しない。
