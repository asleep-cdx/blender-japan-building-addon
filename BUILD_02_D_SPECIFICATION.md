# 日本住宅モデラー — Build 02-D Specification

## 1. 目的

Build 02-Dでは、Build 02-Cまでに実装した壁作図機能へ、**離れた既存Wall端点を基準にしたX/Yアライメントスナップ（同一X座標・同一Y座標への位置合わせ）**を追加する。

これにより、既存Wall端点そのものへ直接スナップできない距離でも、別のWall端点と同じX座標またはY座標へ新しい壁の始点・終点を正確に揃えられるようにする。

代表的な用途は、四角形・長方形の部屋を作る際に、離れた2本の壁の端点を正確に同じX/Y座標へ揃えることである。

あわせて、プロジェクト既定値として新規Wallのデフォルト壁厚を **130 mm** へ変更する。

Build 02-Dでは、Edit Modeで手動編集されたMeshと `jhm_wall.start` / `jhm_wall.end` の同期や、壁メッシュ同士の接合処理は行わない。

---

## 2. 基準状態

Build 02-DはGitHub `main` の以下の状態を基準とする。

- Build 02-C実装コミット:
  - `7a3beee Implement Build 02-C endpoint snapping`
- Build 02-Cの直接端点スナップ距離:
  - `_SNAP_DISTANCE_PX = 16.0`

Build 02-Cの実機テストは完了済みであり、16 pxの直接端点スナップを正式仕様として維持する。

---

## 3. 親仕様

本仕様書は以下を前提とする。

- `SPECIFICATION.md`
- `BUILD_02_A_SPECIFICATION.md`
- `BUILD_02_B_SPECIFICATION.md`
- `BUILD_02_C_SPECIFICATION.md`

既存仕様と矛盾する場合は、勝手に仕様変更せず問題点を明示する。

---

## 4. Build 02-Dで実装する範囲

Build 02-Dでは以下を実装する。

- 既存Wall端点を基準にしたX座標アライメント
- 既存Wall端点を基準にしたY座標アライメント
- 始点確定前のX/Yアライメント
- 始点確定後の終点候補X/Yアライメント
- X/Yアライメント候補の画面ピクセル基準判定
- アライメント基準端点のハイライト
- X/Yアライメントガイド表示
- 直接端点スナップとの優先順位制御
- Shift 15°角度拘束との共存
- Shift押下・解放時の即時再評価
- Build 02-Cの16 px直接端点スナップ維持
- デフォルト壁厚を105 mmから130 mmへ変更
- 壁高さデフォルト2500 mmの維持
- Build 02-A / 02-B / 02-Cの既存機能維持

---

## 5. Build 02-Dで実装しない範囲

以下はBuild 02-Dでは実装しない。

- Edit Modeで変更されたMeshから `start` / `end` を逆算する処理
- Edit ModeとWallデータの自動同期
- Wall専用の端点編集UI
- 既存Wallの壁厚・高さ編集と再生成
- 壁途中への直接スナップ
- 任意Mesh頂点への一般スナップ
- グリッドスナップ
- 壁芯線分どうしの自動交点スナップ
- T字接合
- 十字接合
- 出隅・入隅
- 壁厚を考慮した端部トリミング
- 壁同士の自動延長
- 「接合する / しない」の接続属性
- ドア・窓・床・天井
- 間取り図自動解析
- その他後続Buildの機能

---

# Part A — X/Yアライメントスナップ

## 6. 用語

### 6.1 直接端点スナップ

Build 02-Cで実装済みの、カーソルが既存Wall端点そのものへ近づいた場合に、その端点座標へ完全一致させる機能。

直接端点スナップ距離は引き続き、

`16 px`

とする。

### 6.2 Xアライメント

既存Wall端点PのX座標と、作図候補点EのX座標を一致させる。

`E.x = P.x`

とする。

画面上では、PとEを結ぶガイドはワールドY方向の線となる。

### 6.3 Yアライメント

既存Wall端点PのY座標と、作図候補点EのY座標を一致させる。

`E.y = P.y`

とする。

画面上では、PとEを結ぶガイドはワールドX方向の線となる。

---

## 7. アライメント参照端点

X/Yアライメントの参照元は、Build 02-Cと同じく日本住宅モデラーが管理する既存Wallの保存済み端点のみとする。

対象：

- `object.jhm_wall.is_wall == True`
- `object.jhm_wall.start`
- `object.jhm_wall.end`

メッシュ頂点や見た目上の端部位置を参照してはならない。

---

## 8. 表示状態による対象除外

以下のWall端点はX/Yアライメント参照対象外とする。

- Viewportで非表示
- 非表示Collection内
- 現在のView Layerから除外
- Local View等で現在視認できない
- 画面2D位置へ投影不能
- 現在の3D Viewの表示領域外にある参照端点

Build 02-Cと同じ可視性判定方針を維持する。

---

## 9. アライメント判定距離

X/Yアライメントの初期判定距離は、

`10 px`

とする。

想定定数：

`_ALIGN_DISTANCE_PX = 10.0`

直接端点スナップの16 pxより狭くし、長く伸びるX/Yガイドが過剰に候補を奪わないようにする。

この値はBuild 02-DではUI設定化しない。

実機テスト後、操作感に応じて定数を調整できる構造にしておく。

---

## 10. Xアライメント候補の判定

raw候補をR、参照端点をPとする。

Shift未押下時のXアライメント候補は、

`C_x = (P.x, R.y, 0)`

とする。

RとC_xを現在の3D Viewへ投影し、画面距離が `_ALIGN_DISTANCE_PX` 以下ならXアライメント候補として扱う。

つまり、参照端点Pから無限に伸びるワールドY方向ガイドへカーソルが近づいたときに、X座標をPへ一致させる。

---

## 11. Yアライメント候補の判定

raw候補をR、参照端点をPとする。

Shift未押下時のYアライメント候補は、

`C_y = (R.x, P.y, 0)`

とする。

RとC_yを現在の3D Viewへ投影し、画面距離が `_ALIGN_DISTANCE_PX` 以下ならYアライメント候補として扱う。

つまり、参照端点Pから無限に伸びるワールドX方向ガイドへカーソルが近づいたときに、Y座標をPへ一致させる。

---

## 12. 距離に依存しない位置合わせ

参照端点Pと作図候補点Rのワールド距離は、X/Yアライメント可否の判定条件にしない。

参照端点が数m離れていても、

- 同一Xガイドへ画面上で近づいた
- 同一Yガイドへ画面上で近づいた

場合はアライメント可能とする。

これがBuild 02-Dの主要目的である。

---

## 13. 始点確定前のアライメント

`＋ 壁` を開始し、まだ始点をクリックしていない状態でもX/Yアライメントを有効にする。

優先順位：

1. 既存Wall端点への直接スナップ
2. X/Yアライメント
3. raw XY

始点確定前にはShift角度拘束は適用しない。

### 13.1 Xのみ成立

始点候補のXを参照端点のXへ一致させる。

### 13.2 Yのみ成立

始点候補のYを参照端点のYへ一致させる。

### 13.3 XとYが同時成立

X候補とY候補を独立に選び、

`start = (X参照値, Y参照値, 0)`

として両方を同時適用してよい。

XとYの参照元は同じ端点である必要はない。

---

## 14. 終点候補のアライメント

始点確定後もX/Yアライメントを有効にする。

Shift未押下時は、

1. 直接端点スナップ
2. X/Yアライメント
3. 自由角度

の順に評価する。

XとYの両方が成立する場合は、両座標を同時に一致させてよい。

---

## 15. 複数のX候補がある場合

複数の参照端点がXアライメント候補になる場合は、raw候補から画面上の補正距離が最小となる候補を採用する。

完全同距離の場合は決定順を安定させ、マウスが停止しているのに候補が交互に切り替わることを避ける。

---

## 16. 複数のY候補がある場合

複数の参照端点がYアライメント候補になる場合も、raw候補から画面上の補正距離が最小となる候補を採用する。

完全同距離の場合は安定した決定順を使用する。

---

## 17. X/Y同時アライメント

Shift未押下時にX候補とY候補が同時に成立した場合、

- Xは最良のX参照端点
- Yは最良のY参照端点

を使用する。

最終候補：

`E = (X_reference.x, Y_reference.y, 0)`

とする。

この場合、2本のアライメントガイドを表示する。

---

# Part B — 直接端点スナップとの優先順位

## 18. 直接端点スナップを最優先する

Build 02-Cの直接端点スナップは、Build 02-Dでも最優先とする。

優先順位：

**直接端点スナップ ＞ X/Yアライメント ＞ 通常の自由候補**

直接端点スナップが成立した場合は、

- X/Yアライメントで座標を変更しない
- Shift角度拘束で座標を変更しない
- 保存済み既存Wall端点座標へ完全一致させる

こと。

---

## 19. 直接端点スナップ距離

直接端点スナップ距離はBuild 02-C実機テストで確定した、

`16 px`

を維持する。

現在コード中の直接端点スナップdocstring等に「12 screen pixels」という旧表記が残っている場合は、挙動を変えず **16** へ修正する。

---

# Part C — Shift 15°拘束との共存

## 20. 基本方針

Shift 15°拘束とX/Yアライメントは、可能な場合は**同時成立**させる。

ただし直接端点スナップが成立した場合は直接端点スナップを最優先する。

概念上の処理：

`raw XY`
→ 直接端点スナップ判定
→ 成功なら確定候補
→ 失敗ならShift状態を確認
→ Shiftなし: X/Yアライメント
→ Shiftあり: 15°拘束線とX/Yガイドの交点候補
→ preview / final

---

## 21. Shiftなし

Shift未押下時：

1. 直接端点スナップ
2. X/Yアライメント
3. 自由候補

とする。

---

## 22. Shiftあり — 15°拘束線

Build 02-Bと同様に、始点Sからraw候補Rへの方向を最も近い15°刻みに丸める。

拘束方向単位ベクトルをUとする。

通常のShift候補は従来どおり、R-SをUへ投影した点とする。

---

## 23. Shift + Xアライメント

X参照端点Pがある場合、

`E.x = P.x`

かつ

`E = S + tU`

を満たす交点を求める。

`U.x` がゼロに近い場合、原則としてXガイドとの一意な交点を求められないため、そのXアライメント候補は無効とする。

ただし既に `S.x == P.x` とみなせる場合は、拘束線自体がXガイド上にあるため、追加のX補正を必要としない。

交点パラメータ：

`t = (P.x - S.x) / U.x`

とする。

raw候補方向と反対側となる `t < 0` の交点は候補から除外する。

交点を画面投影し、raw候補との画面距離が `_ALIGN_DISTANCE_PX` 以下の場合のみShift + Xアライメント候補として扱う。

---

## 24. Shift + Yアライメント

Y参照端点Pがある場合、

`E.y = P.y`

かつ

`E = S + tU`

を満たす交点を求める。

`U.y` がゼロに近い場合、原則としてYガイドとの一意な交点を求められないため、そのYアライメント候補は無効とする。

ただし既に `S.y == P.y` とみなせる場合は、拘束線自体がYガイド上にあるため、追加のY補正を必要としない。

交点パラメータ：

`t = (P.y - S.y) / U.y`

とする。

`t < 0` の交点は候補から除外する。

交点を画面投影し、raw候補との画面距離が `_ALIGN_DISTANCE_PX` 以下の場合のみShift + Yアライメント候補として扱う。

---

## 25. Shift中にX/Y両候補が成立する場合

Shift 15°拘束線は1本なので、通常はXガイド交点またはYガイド交点のどちらかを採用する。

両方が有効な場合は、

- raw候補との画面距離が小さい交点

を採用する。

Shift中に、互いに異なるX参照・Y参照を同時に満たすために15°角度を破ってはならない。

---

## 26. Shift中にアライメント候補がない場合

Build 02-B / 02-Cと同じ15°拘束へ戻る。

アライメント機能追加によってShift単独の挙動を変えてはならない。

---

## 27. Shift押下・解放時の即時更新

Build 02-B / 02-Cの仕様を維持する。

最後のraw XY候補を保持し、マウスを動かさなくてもShift押下・解放イベントで、

- 直接端点スナップ
- X/Yアライメント
- Shift 15°拘束

を再評価する。

---

# Part D — アライメントガイド表示

## 28. ガイド表示の目的

ユーザーが、

- どの既存端点を基準にしているか
- XとYのどちらを揃えているか
- なぜその位置に吸着しているか

を視覚的に判断できるようにする。

---

## 29. Xアライメントガイド

Xアライメント成立時は、

- 基準端点P
- 現在のアライメント候補E

を結ぶガイドをGPU描画する。

PとEは同一X座標なので、ワールドY方向のガイドとなる。

画面上の見え方はViewport角度に依存してよい。

---

## 30. Yアライメントガイド

Yアライメント成立時も、

- 基準端点P
- 現在のアライメント候補E

を結ぶガイドをGPU描画する。

PとEは同一Y座標なので、ワールドX方向のガイドとなる。

---

## 31. ガイドの見た目

推奨：

- 画面ピクセル基準の細線
- 約1 px
- 壁芯プレビューより補助線らしい弱い表示
- 可能なら破線または点線
- 前面表示
- Object / Mesh / Curve / Emptyを生成しない

破線実装が不必要に複雑になる場合は、Build 02-Dでは細い実線でもよい。

機能の正確性を優先する。

---

## 32. 参照端点のハイライト

X/Yアライメントで使用中の参照端点は、Build 02-Cのスナップ強調表示に似たリングでハイライトする。

表示イメージ：

`◎・・・・・・・・・・・・○`

- `◎` = アライメント参照端点
- `・・・・` = ガイド
- `○` = 現在の候補点

直接端点スナップと同じ色調を使用してよい。

---

## 33. X/Y同時アライメント表示

Shift未押下時にXとYの両方が成立した場合は、

- X参照端点
- Y参照端点
- Xガイド
- Yガイド

を同時表示する。

同じ端点が両方の参照元であれば、ハイライトは重複描画しなくてよい。

---

## 34. 始点確定前のガイド

始点クリック前にも、X/Yアライメントが成立している場合はガイドと参照端点ハイライトを表示する。

これによりクリック前に位置合わせ結果を確認できる。

---

## 35. 始点確定後

始点が確定したら、始点確定前の一時的なX/Yアライメントガイドは消してよい。

終点候補に対する現在のガイドだけを表示する。

Build 02-Cの「直接スナップ済み始点」の表示は既存仕様を維持してよい。

---

# Part E — デフォルト壁厚

## 36. 新規Wallデフォルト壁厚

新規Wallのデフォルト壁厚を、

`105.0 mm`

から

`130.0 mm`

へ変更する。

対象：

`JHM_NewWallDefaults.wall_thickness`

想定変更：

`default=130.0`

---

## 37. Wallプロパティ自体のデフォルト

内部整合性のため、

`JHM_WallProperties.wall_thickness`

のデフォルト値も、

`105.0`

から

`130.0`

へ変更する。

通常の作図では作図開始時のスナップショット値が保存される既存仕様を維持する。

---

## 38. 壁高さ

壁高さデフォルトは変更しない。

`2500.0 mm`

を維持する。

---

## 39. 既存Wallへの影響

デフォルト値変更は、既に作成済みのWallの保存済み `wall_thickness` を書き換えてはならない。

既存 `.blend` 内のWallを130 mmへ自動移行しない。

既存シーンでユーザーが新規壁デフォルト値を明示的に変更・保存している場合も、その保存済み値を強制的に130 mmへ上書きする移行処理はBuild 02-Dでは実装しない。

130 mmは**新しいデフォルト値**として扱う。

---

# Part F — Edit ModeとSource of Truth

## 40. Source of Truth

Build 02-DでもWallの正規データは、

- `jhm_wall.start`
- `jhm_wall.end`
- `jhm_wall.wall_thickness`
- `jhm_wall.wall_height`

とする。

MeshからWallデータを逆算しない。

---

## 41. Edit Modeでの手動Mesh編集

ユーザーがEdit ModeでWall Meshの頂点を移動しても、

`jhm_wall.start / end`

は自動更新されない。

そのため見た目のMesh端点とスナップ・アライメント参照点がずれる可能性がある。

これはBuild 02-Dの範囲外とする。

---

## 42. 今後のWall編集方針

将来のWall編集では、

`Wall data`
→ `Mesh regenerate`

という一方向の構造を維持する。

予定機能：

- 選択Wallの始点編集
- 選択Wallの終点編集
- 壁厚編集
- 壁高さ編集
- 対象WallだけMesh再生成

Build 02-Dでは先取り実装しない。

---

# Part G — 実装責務と変更ファイル

## 43. 主要変更対象

Build 02-Dの主要変更対象は、

- `japanese_house_modeler/operators.py`
- `japanese_house_modeler/properties.py`

とする。

`operators.py`:
- X/Yアライメント
- Shiftとの交点処理
- ガイド描画
- 状態管理

`properties.py`:
- 壁厚デフォルト130 mm

---

## 44. 原則変更しないファイル

必要がない限り以下を変更しない。

- `japanese_house_modeler/__init__.py`
- `japanese_house_modeler/ui.py`

UIは既存プロパティ値を表示しているため、デフォルト壁厚変更のためだけにUIコードを変更しない。

---

## 45. 不要なリファクタリング禁止

Build 02-D実装のために必要のない、

- 全面リファクタリング
- 変数名総変更
- ファイル分割
- 大規模クラス追加
- コードフォーマットだけの変更

は行わない。

Build 02-Cまでの動作確認済みコードへの影響を最小化する。

---

# Part H — 候補状態

## 46. 実装内部で保持してよい状態

実装上必要であれば、Operator内部に以下に相当する一時状態を保持してよい。

- 直接端点スナップ候補
- Xアライメント参照端点
- Yアライメント参照端点
- 現在のアライメント候補点
- 現在有効なガイド種別
- 現在のShift状態
- 最後のraw XY候補

これらは作図中だけの一時状態とする。

Object Propertyとして永続保存する必要はない。

---

## 47. 確定Wallへ保存する値

Wall確定時は、最終的に解決された正確な候補点を、

- `wall.start`
- `wall.end`

へ保存する。

X/Yアライメントが成立した場合は、参照端点のXまたはY値と数値的に一致すること。

見た目だけ揃っている状態にしてはならない。

---

# Part I — 既存機能維持

## 48. Build 02-C機能維持

以下を維持する。

- 直接端点スナップ16 px
- 始点直接スナップ
- 終点直接スナップ
- 直接スナップ候補ハイライト
- 非表示Wall除外
- 直接端点スナップ ＞ Shift拘束
- スナップ解除時の自然な復帰

---

## 49. Build 02-B機能維持

以下を維持する。

- Shift未押下時の自由角度
- Shift押下時15°刻み拘束
- Shift押下・解放の即時更新
- 細い壁芯プレビュー
- 始点・終点マーカー
- 前面GPU表示

---

## 50. Build 02-A機能維持

以下を維持する。

- XY平面レイ交差
- Wallデータ構造
- 壁芯基準Mesh
- 作図開始時の壁厚・高さスナップショット
- 技術的最小長
- Undo
- Esc / 右クリックキャンセル
- Wall生成後の選択・active化
- 確定前Object / Mesh非生成
- 一つのWall = 一つのObject

---

# Part J — 実機テスト

## 51. テストA — 離れたY位置合わせ

1. 左側に縦Wallを作る
2. 右側に離れた位置で別の縦Wallを作る
3. 右側Wallの終点を左側Wall終点のYガイド付近へ移動
4. Yアライメントガイドが表示される
5. クリックして確定
6. 2本の終点Y座標が一致する

四角形の下辺を後から直接端点スナップで結んだ場合、下辺が正確な水平線になることを確認する。

---

## 52. テストB — 離れたX位置合わせ

上記を90°回転した状況で確認する。

離れた端点どうしのX座標が正確に一致すること。

---

## 53. テストC — 始点アライメント

1. `＋ 壁` を開始
2. 始点クリック前に離れた既存端点のXまたはYガイドへ近づける
3. ガイドと参照端点ハイライトが表示される
4. クリック
5. 新規Wallの始点が該当X/Y座標へ一致する

---

## 54. テストD — X/Y同時アライメント

1. X参照に使える端点を用意
2. Y参照に使える別端点を用意
3. 両ガイドの交点付近へカーソルを移動
4. 2本のガイドが表示される
5. 候補点が両方の座標を満たす
6. 確定後の保存値も一致する

---

## 55. テストE — 直接端点スナップ優先

1. X/Yガイドが成立する状態を作る
2. そのうえで既存端点16 px以内へカーソルを移動
3. 直接端点スナップが優先される
4. Shift有無に関係なく端点座標へ完全一致する

---

## 56. テストF — Shift + Yアライメント

矩形作図の代表テスト。

1. 上辺Wallを作る
2. 左側縦Wallを下向きに作り、下端を確定
3. 右上端点から新規Wallを開始
4. Shiftで90° / 270°方向へ縦拘束
5. 左下端点のYガイド付近へ終点を移動
6. 右側Wallが縦方向を維持したまま左下端点と同じYで止まる
7. 確定
8. 左右の下端Y座標が一致する

---

## 57. テストG — Shift + Xアライメント

テストFを90°回転した構成で確認する。

Shift角度とX位置合わせが同時成立すること。

---

## 58. テストH — Shiftのみ

X/Yアライメント候補がない位置で、

- 0°
- 15°
- 30°
- 45°
- 90°

等の既存Shift拘束がBuild 02-C以前と同じように動くこと。

---

## 59. テストI — スナップ解除

X/Yアライメント中にカーソルをガイド判定距離外へ移動する。

- Shiftなし → 自由候補へ戻る
- Shiftあり → 15°拘束へ戻る

即時更新されること。

---

## 60. テストJ — 非表示Wall

非表示Wallの端点からX/Yガイドが発生しないこと。

---

## 61. テストK — デフォルト壁厚

新しいデフォルト状態で、

- 壁厚 = 130 mm
- 壁高さ = 2500 mm

となることを確認する。

130 mmで作図したWallの実Mesh厚も130 mmであること。

---

## 62. テストL — 既存Wall保持

既に別の壁厚を持つWallが存在する場合、その保存済み壁厚がデフォルト変更によって勝手に130 mmへ変化しないこと。

---

## 63. 回帰テスト

最低限以下を確認する。

- 自由角度
- Shift 15°
- 直接端点スナップ
- 16 pxの操作感
- 直接スナップハイライト
- Esc
- 右クリック
- Ctrl+Z
- Wall生成後選択
- 非表示Wall除外
- 壁芯プレビュー
- 壁高さ2500 mm
- XY平面への作図

---

# Part K — 完了条件

## 64. Build 02-D完了条件

Blender 5.2 LTS実機で以下を満たした時点でBuild 02-D完了とする。

- 離れた既存Wall端点と同じXへ正確に揃えられる
- 離れた既存Wall端点と同じYへ正確に揃えられる
- 始点でもX/Yアライメントできる
- 終点でもX/Yアライメントできる
- アライメントガイドが表示される
- 参照端点が視覚的に分かる
- 直接端点スナップ16 pxが最優先で維持される
- Shift 15°とX/Yアライメントが可能な場合に同時成立する
- ガイド範囲外では従来挙動へ自然に戻る
- 非表示Wallを参照しない
- 保存されたstart/endがアライメント座標と正確に一致する
- 新規壁厚デフォルトが130 mm
- 壁高さデフォルトが2500 mmのまま
- 既存Wallの保存値を勝手に変更しない
- Build 02-A〜02-Cの既存機能に回帰不具合がない
- 確定前に不要なObject / Meshを生成しない

---

# Part L — 次段階

## 65. Build 02-D後に検討する機能

Build 02-D完了後は、基本作図精度が十分になった段階として、以下を順次検討する。

- Wall専用端点編集
- 壁厚・壁高さ変更と対象Wall再生成
- Wall端点ごとの接続情報
- 「接合する / しない」の制御
- T字接合
- 壁芯交点処理
- 出隅・入隅
- 壁厚を考慮した端部自動トリミング

Build 02-Dではこれらを先取り実装しない。
