# 日本住宅モデラー — Build 02-B Specification

## 1. 目的

Build 02-Bでは、Build 02-Aで実装した自由作図を維持したまま、作図補助として角度拘束を追加する。

同時に、確定前プレビューを壁厚を持つ立体表示から、壁芯を示す細いラインと始点・終点マーカーへ変更する。

## 2. 親仕様

本仕様書は以下を前提とする。

- `SPECIFICATION.md`
- `BUILD_02_A_SPECIFICATION.md`

既存仕様と矛盾する場合は、勝手に仕様変更せず問題点を明示する。

## 3. Build 02-Bで実装する範囲

Build 02-Bでは以下を実装する。

- Shift押下中のみ15度刻みの角度拘束
- Shift未押下時の完全自由作図
- Shift押下・解放時の即時プレビュー更新
- 壁芯ラインによる細い作図プレビュー
- 始点・終点候補の円形マーカー表示
- Build 02-Aの候補解決順序の維持
- 拘束後終点に対する最小長判定
- Build 02-Aの確定Wall生成・Undo・キャンセル等の維持

## 4. Build 02-Bで実装しない範囲

以下はBuild 02-Bでは実装しない。

- 既存Wallへのスナップ
- 既存Wall端点へのスナップ
- 壁同士の自動接合
- 壁同士の自動トリミング
- 出隅・入隅処理
- 既存Wallの端点編集
- 既存Wallの壁厚・壁高さ変更と再生成
- 床・天井・ドア・窓
- 間取り図の自動解析
- その他Build 02-C以降の機能

## 5. 通常時の作図

Shiftを押していない場合、終点候補はBuild 02-Aと同様に自由角度とする。

自動的な角度吸着は行わない。

## 6. Shiftによる角度拘束

Shift押下中のみ角度拘束を有効にする。

拘束角度はワールドXY平面のX軸を基準とし、15度刻みとする。

例：

- 0°
- 15°
- 30°
- 45°
- 60°
- 75°
- 90°
- 105°
- 120°
- 135°
- 150°
- 165°
- 180°

以後も同様に全方向を扱う。

0° / 180° は水平、90° / 270° は垂直となる。

Shiftを離した場合は、即座に自由角度へ戻る。

## 7. Shift押下・解放時の即時更新

始点確定後は、MOUSEMOVEだけでなくShiftキーの押下・解放イベントでも最後に取得したraw XY終点候補を再解決する。

- Shift押下：即座に15度刻み拘束へ切り替える
- Shift解放：即座に自由角度へ戻す

マウスを追加で動かすことを要求しない。

## 8. 候補解決順序

Build 02-Aで確立した以下の順序を維持する。

`raw XY座標`
→ `snap_endpoint_candidate()`
→ `resolve_endpoint_candidate()`
→ `preview / final endpoint`

Build 02-Bでは、

- `snap_endpoint_candidate()` は入力候補をそのまま返し、既存Wallスナップを行わない
- `resolve_endpoint_candidate()` に角度拘束を実装する

Build 02-Cで既存Wall端点スナップを追加できる責務分離を維持する。

## 9. 角度拘束の計算

始点を S、候補点を R とする。

Shift未押下時は、候補を変更しない。

Shift押下時は、

1. `D = R - S`
2. D のXY平面上の角度を求める
3. 最も近い15度刻みの角度へ丸める
4. その拘束方向へDを投影する
5. 拘束後終点 E を求める

始点Sは変更しない。

確定時に保存するWallの `end` は最終的なEとする。

## 10. 最小長判定

最小長判定は拘束前のraw候補ではなく、候補解決後の最終終点Eに対して行う。

`distance(E - S) > 1e-6 m`

を有効条件とする。

`1e-6 m` 以下の場合は、

- 有効な壁プレビューとして扱わない
- 2回目のクリックでもWallを生成しない

この値は建築上の最小寸法ではなく、退化メッシュ防止の技術的しきい値である。

## 11. プレビュー表示

Build 02-Aの壁厚を反映した立体プレビューは廃止する。

確定前は以下のみをGPU描画する。

`○────────────────○`

- 左の○：始点
- 右の○：現在の終点候補
- 中央の細い線：壁芯

## 12. 壁芯ライン

壁芯ラインは、

- startとend_candidateを結ぶ位置
- 画面ピクセル基準の一定幅
- 目安として1〜2 px程度
- 壁厚・壁高さに依存しない

ものとする。

ズームによって壁厚に対応する太さへ変化させない。

## 13. 始点・終点マーカー

始点・終点候補には円形マーカーを表示する。

- ○型
- 画面ピクセル基準の一定サイズ
- 目安として半径5〜6 px程度
- 壁厚・壁高さに依存しない

## 14. プレビューの前後関係

プレビューは既存Wall、床、参照物等に隠れず、作図中に常に視認できる前面表示とする。

プレビューはGPU描画のみとし、確定前に以下を生成してはならない。

- Object
- Mesh
- Collection

## 15. 確定Wall

2回目の左クリックで終点を確定した場合、Build 02-Aと同様に実際のWallメッシュを生成する。

確定Wallは、

- 壁芯：start / end
- 壁厚：作図開始時のスナップショット値
- 壁高さ：作図開始時のスナップショット値

から生成する。

プレビュー線の太さは確定Wallの壁厚には影響しない。

## 16. Build 02-A仕様の維持

以下を変更しない。

- XY平面へのレイ交差
- Wallデータ構造
- `start`
- `end`
- `is_wall`
- `wall_thickness`
- `wall_height`
- 壁芯基準の確定メッシュ
- 作図開始時の壁厚・壁高さスナップショット
- 技術的最小長
- Undo
- Wall生成後の選択・active化
- Esc / 右クリックによるキャンセル
- read-onlyの選択中Wall表示
- 確定前のObject / Mesh非生成

## 17. 将来互換性

将来的に以下を追加する予定である。

- 既存Wall端点へのスナップ
- Wallの壁厚・壁高さ変更と再生成
- Wallの端点編集
- 壁同士の接合
- 出隅・入隅処理

そのためBuild 02-Bでは、Wallの `start` / `end` / `wall_thickness` / `wall_height` を将来のWall Systemが利用できる構造を維持する。

## 18. 完了条件

Blender 5.2 LTS実機で、少なくとも以下を確認する。

- Shift未押下時は自由角度で作図できる
- Shift押下時は15度刻みに拘束される
- Shift押下だけでプレビューが即時更新される
- Shift解放だけで自由角度へ即時復帰する
- プレビューが細い壁芯ライン＋両端○になる
- プレビューが壁厚に依存しない
- 確定Wallの壁厚・高さがBuild 02-A同様に正しい
- Esc / 右クリックでキャンセルできる
- Ctrl+Zで直前のWall生成をUndoできる
